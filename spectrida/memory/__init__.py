"""SpectrIDA semantic memory — cross-binary function recognition.

Every function SpectrIDA names becomes a learned semantic fingerprint,
gated by emulation-verified correctness, stored in a cross-binary index.
The system recognizes reused code across compilers, architectures, and titles,
and gets measurably better with every binary run through it.
"""
